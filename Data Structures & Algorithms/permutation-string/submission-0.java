class Solution {
    public boolean checkInclusion(String s1, String s2) {
        if(s1.length() && s2.length() == null) return false;
         
         while(s1.length() != s2.length()) {
            if (s2.contains(s1))
         }

    }
}
